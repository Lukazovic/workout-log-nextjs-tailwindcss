import { render, screen } from '@testing-library/react'

import {{pascalCase name}}Layout from '.'

describe('<{{pascalCase name}} />', () => {
  it('should render the heading', () => {
    render(<{{pascalCase name}}Layout />)

    expect(screen.getByRole('heading', { name: /{{pascalCase name}}Layout/i })).toBeInTheDocument()
  })
})
